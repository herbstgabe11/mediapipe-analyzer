services:
  - type: web
    name: mediapipe-analyzer
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: REPLICATE_API_TOKEN
        sync: false
